/**
 * Angular router module for defining component routes
 */
import { Routes } from "@angular/router";

/**
 * Component routes configuration
 * This array defines routes for components that are lazy-loaded
 * Currently empty as routes are defined in the main app-routing.module.ts
 */
export const ComponentsRoutes: Routes = [
  // Routes can be added here for lazy-loaded components
];
