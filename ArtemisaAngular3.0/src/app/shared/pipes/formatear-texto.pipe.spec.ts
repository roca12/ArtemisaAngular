import { FormatearTextoPipe } from './formatear-texto.pipe';

describe('FormatearTextoPipe', () => {
  it('create an instance', () => {
    const pipe = new FormatearTextoPipe();
    expect(pipe).toBeTruthy();
  });
});
